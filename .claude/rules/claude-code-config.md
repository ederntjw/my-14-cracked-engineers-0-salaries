---
description: Triggers when working on Claude Code configuration files. Loads best practices reference.
paths:
  - "CLAUDE.md"
  - "CLAUDE.local.md"
  - ".claude/**"
  - ".mcp.json"
  - ".mcp.json.template"
---

When writing, editing, or reviewing any Claude Code configuration (CLAUDE.md, rules, skills, hooks, commands, settings.json, or MCP servers), read `docs/CLAUDE-CODE-BEST-PRACTICES.md` first.

Apply its guidance for line limits, structure, what to include vs exclude, and when to use rules vs skills vs hooks.
