# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Session state (auto-generated, not project data)
context/.last-session

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build output
dist/
build/
.next/
out/

# Test coverage
coverage/

# Claude memory (per-user, not project data)
# Memory is stored in ~/.claude/projects/ by folder path

# Graphify output (generated, not source)
graphify-out/

# MemPalace data (per-user, stored in ~/.mempalace/)
# The palace itself lives outside the repo — only mempalace.yaml config is tracked
